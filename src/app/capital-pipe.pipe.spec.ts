import { CapitalPipePipe } from './capital-pipe.pipe';

describe('CapitalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
